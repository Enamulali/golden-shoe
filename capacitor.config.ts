import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.golden.shoe.app',
  appName: 'golden-shoe',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
